# Copy to .env. The Lineup web app that serves /api/public/sites.
LINEUP_API_URL=http://localhost:3000
# Optional: serve shops at <slug>.<domain>.
SITES_ROOT_DOMAIN=
# Optional: Vercel "Protection Bypass for Automation" secret, if the web app is behind Vercel Authentication.
LINEUP_API_BYPASS=
